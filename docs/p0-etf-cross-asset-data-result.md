# P0 ETF 跨资产数据 v1 结果

结论：**DATA_GAP，不允许启动策略收益研究，严格策略计数仍为 `0/5`。**

冻结主表包含 421 只 ETF，其中 90 只后来退市。TickFlow 日线只覆盖 313 只，覆盖率 74.35%；108 只缺失标的中 89 只是后来退市的 ETF。直接使用这批日线会系统性删除失败标的，构成严重幸存者偏差。

复权因子覆盖全部 313 只有日线的 ETF，OHLCV 非法行数为 0。原始审计把 420 只有复权的证券直接除以 313 只有日线的证券，错误显示 134.19%；正确的交集口径是 313/313，即 100%。该统计缺陷不改变 `DATA_GAP` 结论，代码已修正。

替代链路的只读探针已验证：Tushare `fund_daily` 能返回一只 2020 年退市 ETF 在上市期间的 86 根日线。下一版只补齐 v1 缺失证券，不移除退市样本、不降低 95% 门槛。

## 证据

- 服务器审计：`/mnt/data/tick-stock-panel/data/research/p0_etf_cross_asset_data_audit.json`
- 原始审计 SHA-256：`c6d8e9b70a3c9183f560efbe754b999fbac6a60d084792372372b0a06dd45334`
- 主表 421 只、退市 90 只、日线 248,124 行 / 313 只、复权 332,756 行 / 420 只。
- `validation_returns_read=false`，尚未产生任何 ETF 策略收益。

# P0 中国商品期货数据 v2 结果

结论：**连续行情与主力映射 DATA_QUALIFIED，但尚不能冻结可执行策略；严格计数仍为 `0/5`。**

20 个冻结品种共有 34,089 条主力连续日线和同量主力映射，所有品种至少 1,656 根，日期映射覆盖 100%；610 个被映射真实合约的每手数量均有效，行情无重复键和非法活跃价格。

初次审计漏掉甲醇旧代码 `ME` 的 3 个合约，原因是用今天根代码 `MA` 过滤历史主表。v2 改为先读取逐日映射，再按真实合约集合保留主表，完整保留代码迁移历史。

连续线只可用于因果信号，不能用于换月撮合。下一层必须补齐 610 个真实合约日线，验证每个映射日和换月日都能用旧/新合约真实价格执行。

## 证据

- 服务器审计：`/mnt/data/tick-stock-panel/data/research/p0_cn_commodity_futures_data_audit.json`
- SHA-256：`d40014067a8c19d5846b8ba609f72ea2e74a620409bd7ed82c8a2ee1a91a3028`
- `validation_returns_read=false`。
